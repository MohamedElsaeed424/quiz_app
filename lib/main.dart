import 'package:flutter/material.dart';
import 'package:quiz_app/general_container.dart';

void main() {
  runApp(const MaterialApp(
    home: Scaffold(
      body: GeneralContainer(Colors.white, Colors.blueGrey),
    ),
  ));
}
